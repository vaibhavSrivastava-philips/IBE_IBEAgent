{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }

  // Ibe:Endpoints + Ibe:Contracts (communication + contract topology, FSE-owned) now live in
  // contractData.json. Ibe:Catalog (pipelines/codecs, developer-owned) lives in catalogData.json.
  // Both are merged into configuration by Program.cs alongside this file.
}
